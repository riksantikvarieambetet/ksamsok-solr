
# solr:s base catalogue for settings files 
solr.home=/var/lucene-index

## replication
## following will be used for replacing placeholders of type:
## <!-- @SOLR_REPLICATION@ -->
## PollIntervall för slavar har tagits bort eftersom de läser samma index som mastern
## N.B. take care to use correct syntax for comments.

# xml-snippet for replication master
solr.master.conf=replication master -->\n\
  <requestHandler name="/replication" class="solr.ReplicationHandler" >\n\
    <lst name="master">\n\
      <str name="replicateAfter">commit</str>\n\
      <str name="replicateAfter">startup</str>\n\
      <!--<str name="confFiles">schema.xml,stopwords.txt</str>-->\n\
    </lst>\n\
  </requestHandler>\n\
<!-- replication master

# xml-snippet for replication slave test
solr.slave.test.conf=replication slave -->\n\
  <requestHandler name="/replication" class="solr.ReplicationHandler" >\n\
    <lst name="slave">\n\
      <str name="masterUrl">http://lx-ra-ksamtest1.raa.se:8080/solr/replication</str>\n\
    </lst>\n\
  </requestHandler>\n\
<!-- replication slave

solr.slave.conf=replication slave -->\n\
  <requestHandler name="/replication" class="solr.ReplicationHandler" >\n\
    <lst name="slave">\n\
      <str name="masterUrl">http://lx-ra-ksam1.raa.se:8080/solr/replication</str>\n\
    </lst>\n\
  </requestHandler>\n\
<!-- replication slave
